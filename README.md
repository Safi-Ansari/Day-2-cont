# Day-2-cont

6. 

		       square = Math.sqrt(num);
		       System.out.println("Square of "+num+" is: " + square);
			   
The message is not matching with what you are doing. 'num' is a square number, while 'square' (this name is misleading, it needs to be squareRoot) variable contains square root value.

7. 

a. multicatch - We don't catch NullPointerException. As I mentioned in the class, we always handle it logically, we don't catch it.

b. looks good.

c. looks good.

8. demonstrate with different type. E.g. String or anything other.
